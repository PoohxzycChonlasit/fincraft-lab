# FILE_MAP.md — Map of Files That Currently Exist

This document lists only files and folders that **actually exist** right now. Future structures are listed under "Planned, not created" below.

## Root

| Path | Responsibility | Origin | Next step allowed to change it |
|---|---|---|---|
| `.gitignore` | Ignores `node_modules`, `dist`, `.env`, `fincraft-lab-api/src/generated/prisma/`, etc. | owner-maintained, extended in P2B.1 for the generated Prisma Client path | Update when a new pattern needs ignoring |
| `AGENTS.md` | Canonical rules for every AI tool | created in P0.5 | Update when project-level rules change |
| `CLAUDE.md` | Thin adapter for Claude Code | created in P0.5 | Update only for Claude-specific behavior |
| `GEMINI.md` | Thin adapter for Gemini | created in P0.5 | Update only for Gemini-specific behavior |

## `.agents/skills/fincraft-teacher-stepwise-loop/`

| Path | Responsibility | Origin | Next step allowed to change it |
|---|---|---|---|
| `SKILL.md` | Detailed stepwise loop method (Observe→...→Stop, protocol templates) | owner-provided draft, revised in P0.5, standardized to English in P0_5_ENGLISH_AGENT_DOCUMENTATION_CORRECTION_001 | Update when the loop or project sequence changes |

## `docs/ai/`

| Path | Responsibility | Origin | Next step allowed to change it |
|---|---|---|---|
| `PROJECT_CONTEXT.md` | Relatively stable product context | created in P0.5 | Update when product scope changes |
| `CODING_RULES.md` | Detailed coding rules | created in P0.5 | Update when coding rules change |
| `STEPWISE_WORKFLOW.md` | Lifecycle of each bounded step | created in P0.5 | Update when the workflow changes |
| `CURRENT_STATUS.md` | Latest verified status | created in P0.5 | **Update at the end of every completed step** (evidence-based) |
| `FILE_MAP.md` | This file | created in P0.5 | Update when a new file actually exists |

## `fincraft-lab-api/` (Backend — created in P0)

| Path | Responsibility | Origin | Next step allowed to change it |
|---|---|---|---|
| `package.json` | Dependencies, scripts, `packageManager: pnpm@11.15.1` | generated by `nest new`, 1 line edited in P0, Prisma/PostgreSQL dependencies added in P2A | Edit when a new dependency is needed in a future step |
| `prisma/schema.prisma` | Prisma generator (`moduleFormat = "cjs"`, output to `src/generated/prisma`) and `postgresql` datasource only — no models yet | created in P2B.1 | Add the first model in a future bounded step (not P2B.1) |
| `prisma.config.ts` | Prisma CLI config — schema path, migrations path, `DATABASE_URL` loaded via `env()` | created in P2B.1 | Edit only if the config path or migrations path changes |
| `.env.example` | Documented placeholder for `DATABASE_URL` (no real credentials) | created in P2B.1 | Update if new environment variables are introduced |
| `pnpm-lock.yaml` | Dependency lockfile | generated | Never hand-edit |
| `pnpm-workspace.yaml` | Stores pnpm's `allowBuilds` approval | generated (via `pnpm approve-builds`) | Edit when a new build script needs approval |
| `nest-cli.json` | Nest CLI configuration | generated | Should not be edited at this stage |
| `tsconfig.json`, `tsconfig.build.json` | TypeScript configuration (strict mode) | generated | Should not be edited at this stage |
| `eslint.config.mjs`, `.prettierrc` | Lint/format configuration | generated | Should not be edited at this stage |
| `README.md` | Default README from Nest CLI | generated | Can be updated as the project grows |
| `src/main.ts` | Entry point, app bootstrap | generated | Keep as-is until more config is needed (e.g. CORS) |
| `src/app.module.ts` | Root module — imports `HealthModule` | generated, edited in P1 to import `HealthModule` and drop the generated controller/service | Edit when the next feature module (e.g. Prisma-backed feature) is added |
| `src/health/health.module.ts` | Declares `HealthController`, provides `HealthService` | created in P1 | Edit only if the Health feature's shape changes |
| `src/health/health.controller.ts` | Route prefix `health`; `GET /health` calls `HealthService` and returns its result | created in P1 | Edit only if the Health feature's shape changes |
| `src/health/health.service.ts` | Builds the `{ status, service, timestamp }` response; no database access | created in P1 | Edit only if the Health feature's shape changes |
| `src/health/health.controller.spec.ts` | Unit test: status, service name, valid ISO-8601 timestamp | created in P1 | Extend only if `HealthController`/`HealthService` behavior changes |
| `test/app.e2e-spec.ts`, `test/jest-e2e.json` | e2e test setup — verifies `GET /health` (200) and `GET /` (404) | generated, `app.e2e-spec.ts` rewritten in P1 for the Health feature | Use as the template for future e2e tests |

### Removed in P1

The following generated Hello World files were removed after the Health feature's build/lint/unit/e2e verification passed, per this step's explicit authorization:

- `src/app.controller.ts`
- `src/app.controller.spec.ts`
- `src/app.service.ts`

## `fincraft-lab-web/` (Frontend)

Empty — not yet initialized (out of scope for P0/P0.5).

## Local and generated files (exist on disk, never tracked by Git)

| Path | Responsibility | Why it is not tracked |
|---|---|---|
| `fincraft-lab-api/.env` | Local `DATABASE_URL` (currently a syntactically valid placeholder, not a real credential) | Ignored via the root `.gitignore` (`.env` pattern); never committed |
| `fincraft-lab-api/src/generated/prisma/` | Prisma Client generated by `prisma generate` | Ignored via the root `.gitignore` (path added in P2B.1); regenerated from `schema.prisma`, never hand-edited or committed |

## Planned, not created

The following structures **do not exist yet**. They are a plan derived from `CODING_RULES.md` only — never claim they already exist:

```text
fincraft-lab-api/prisma/migrations/     (no migration created yet — planned for a future bounded step)
fincraft-lab-api/src/database/          (PrismaService/DatabaseModule — planned for P2B.2)
fincraft-lab-api/src/<feature>/         (created one feature at a time per the Initial Project Sequence)

fincraft-lab-web/src/app/
fincraft-lab-web/src/components/
fincraft-lab-web/src/lib/
```

## Related documents

- Current status → [`CURRENT_STATUS.md`](CURRENT_STATUS.md)
- Full step sequence → `.agents/skills/fincraft-teacher-stepwise-loop/SKILL.md`
