# FILE_MAP.md — Map of Files That Currently Exist

This document lists only files and folders that **actually exist** right now. Future structures are listed under "Planned, not created" below.

## Root

| Path | Responsibility | Origin | Next step allowed to change it |
|---|---|---|---|
| `.gitignore` | Ignores `node_modules`, `dist`, `.env`, etc. | owner-maintained | Update when a new pattern needs ignoring |
| `AGENTS.md` | Canonical rules for every AI tool | created in P0.5 | Update when project-level rules change |
| `CLAUDE.md` | Thin adapter for Claude Code | created in P0.5 | Update only for Claude-specific behavior |
| `GEMINI.md` | Thin adapter for Gemini | created in P0.5 | Update only for Gemini-specific behavior |

## `.agents/skills/fincraft-teacher-stepwise-loop/`

| Path | Responsibility | Origin | Next step allowed to change it |
|---|---|---|---|
| `SKILL.md` | Detailed stepwise loop method (Observe→...→Stop, protocol templates) | owner-provided draft, revised in P0.5, standardized to English in P0_5_ENGLISH_AGENT_DOCUMENTATION_CORRECTION_001 | Update when the loop or project sequence changes |

## `docs/ai/`

| Path | Responsibility | Origin | Next step allowed to change it |
|---|---|---|---|
| `PROJECT_CONTEXT.md` | Relatively stable product context | created in P0.5 | Update when product scope changes |
| `CODING_RULES.md` | Detailed coding rules | created in P0.5 | Update when coding rules change |
| `STEPWISE_WORKFLOW.md` | Lifecycle of each bounded step | created in P0.5 | Update when the workflow changes |
| `CURRENT_STATUS.md` | Latest verified status | created in P0.5 | **Update at the end of every completed step** (evidence-based) |
| `FILE_MAP.md` | This file | created in P0.5 | Update when a new file actually exists |

## `fincraft-lab-api/` (Backend — created in P0)

| Path | Responsibility | Origin | Next step allowed to change it |
|---|---|---|---|
| `package.json` | Dependencies, scripts, `packageManager: pnpm@11.15.1` | generated by `nest new`, 1 line edited in P0 | Edit when a new dependency is added (e.g. Prisma) in a future step |
| `pnpm-lock.yaml` | Dependency lockfile | generated | Never hand-edit |
| `pnpm-workspace.yaml` | Stores pnpm's `allowBuilds` approval | generated (via `pnpm approve-builds`) | Edit when a new build script needs approval |
| `nest-cli.json` | Nest CLI configuration | generated | Should not be edited at this stage |
| `tsconfig.json`, `tsconfig.build.json` | TypeScript configuration (strict mode) | generated | Should not be edited at this stage |
| `eslint.config.mjs`, `.prettierrc` | Lint/format configuration | generated | Should not be edited at this stage |
| `README.md` | Default README from Nest CLI | generated | Can be updated as the project grows |
| `src/main.ts` | Entry point, app bootstrap | generated | Keep as-is until more config is needed (e.g. CORS) |
| `src/app.module.ts` | Root module | generated | Edit when the first feature module is added (e.g. a health module) |
| `src/app.controller.ts` | Example controller (`GET /`) | generated | Keep as generated by Nest CLI |
| `src/app.service.ts` | Example service | generated | Keep as generated by Nest CLI |
| `src/app.controller.spec.ts` | Example unit test | generated | Keep as generated by Nest CLI |
| `test/app.e2e-spec.ts`, `test/jest-e2e.json` | e2e test setup | generated | Use as the template for future e2e tests |

## `fincraft-lab-web/` (Frontend)

Empty — not yet initialized (out of scope for P0/P0.5).

## Planned, not created

The following structures **do not exist yet**. They are a plan derived from `CODING_RULES.md` only — never claim they already exist:

```text
fincraft-lab-api/src/health/           (P1 — GET /health)
fincraft-lab-api/prisma/                (Prisma not installed yet)
fincraft-lab-api/src/<feature>/         (created one feature at a time per the Initial Project Sequence)

fincraft-lab-web/src/app/
fincraft-lab-web/src/components/
fincraft-lab-web/src/lib/
```

## Related documents

- Current status → [`CURRENT_STATUS.md`](CURRENT_STATUS.md)
- Full step sequence → `.agents/skills/fincraft-teacher-stepwise-loop/SKILL.md`
